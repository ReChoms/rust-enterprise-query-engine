apiVersion: v1
kind: Service
metadata:
  name: rust-enterprise-query-engine-service
  labels:
    app.kubernetes.io/name: rust-enterprise-query-engine
    app.kubernetes.io/part-of: rust-enterprise-query-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rust-enterprise-query-engine
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
